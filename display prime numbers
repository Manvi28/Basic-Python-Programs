Write a python program to display prime numbers between a range of integers.

start=int(input("enter starting of range: "))
end=int(input("enter end of range: "))
for num in range(start,end+1):
    if num>1:
        for i in range(2,num):
            if num%i==0:
                break
        else:
            print(num)
